{% extends 'base.html.twig' %}

{% block title %}Task index{% endblock %}

{% block body %}
<div class="container-fluid text-center">
    <h1>Task index</h1>
    {% for task in tasks %}
    <div class="row">
        <div class="col">
            <div class="col-md-4 card m-4">
                <div class="card-body">
                    <h2 class="card-title">Tache n° : {{ task.id }}</h2>
                    <p class="card-text">description : {{ task.description }}</p>
                    <div class="card-footer">
                        <small class="text-body-secondary">a faire le : {{ task.dueDate ? task.dueDate|date('Y-m-d H:i:s') : '' }}</small>
                      </div>
                    <span><a href="{{ path('app_task_new') }}">Create new</a></span></br>
                    <span><a href="{{ path('app_task_edit', {'id': task.id}) }}">edit</a></span>
                    <span>{{ include('task/_delete_form.html.twig') }}</span>
                </div>
            </div>
            {% else %}
                <span>no records found</span>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
